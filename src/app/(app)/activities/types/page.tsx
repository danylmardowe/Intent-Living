export default function ActivityTypesIndexPage() {
  return (
    <div className="p-4 max-w-3xl mx-auto">
      <h1 className="text-xl font-semibold">Activity Types – Index</h1>
      <p className="text-sm text-muted-foreground">
        If you can see this, the <code>/activities/types</code> route exists.
        Now navigate to <code>/activities/types/&lt;some-id&gt;</code> to test the dynamic route.
      </p>
    </div>
  )
}
